# (deprecated)

The `examples/` folder is no longer part of the PL Infra Prompt Library. The library itself (in `preview/prompts.json`) holds the canonical examples. Safe to delete this entire folder.
